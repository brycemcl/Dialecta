{
  "name": "@dialecta/db",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "push": "prisma db push --skip-generate",
    "build": "kysely-codegen && tsc"
  },
  "dependencies": {
    "@dialecta/tsconfig": "workspace:*",
    "kysely": "^0.26.3",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1",
    "kysely-codegen":"^0.10.1"
  },
  "devDependencies": {
    "@types/pg": "^8.10.2",
    "prisma": "~5.2.0"
  }
}
