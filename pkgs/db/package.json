{
  "name": "@dialecta/db",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "push": "prisma db push --skip-generate",
    "build": "prisma generate && tsc"
  },
  "dependencies": {
    "@dialecta/tsconfig": "workspace:*",
    "prisma-kysely": "1.7.0",
    "kysely": "^0.26.3"
  },
  "devDependencies": {
    "prisma": "~5.2.0"
  }
}
